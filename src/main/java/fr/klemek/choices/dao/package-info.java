/**
 * Static functions to interact with the datastore.
 * Each function uses classes from the model to write or read values.
 * DaoUtils store package-level functions to transform datastore entities into model objects.
 */
package fr.klemek.choices.dao;